<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_meetup_path(@meetup), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @meetup.title %></h2>
  <p><%= @meetup.description %></p>
</div>
